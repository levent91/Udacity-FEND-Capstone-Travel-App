import { dateValid } from "../js/dateDif.js";
import { dateDif } from "../js/dateDif.js";
import { dateMax } from "../js/dateDif.js";

describe("dateValid() is declared?", () => {
  it("True is the expected value", () => {
    expect(dateValid).toBeDefined();
  });
});
describe("dateValid() is a function?", () => {
  it("True is the expected value", () => {
    expect(typeof dateValid).toBe("function");
  });
});

describe("dateValid() is declared?", () => {
  it("True is the expected value", () => {
    expect(dateValid).toBeDefined();
  });
});
describe("dateValid() is a function?", () => {
  it("True is the expected value", () => {
    expect(typeof dateValid).toBe("function");
  });
});

describe("dateDif() is declared?", () => {
  it("True is the expected value", () => {
    expect(dateDif).toBeDefined();
  });
});
describe("dateDif() is a function?", () => {
  it("True is the expected value", () => {
    expect(typeof dateDif).toBe("function");
  });
});

describe("dateMax() is declared?", () => {
  it("True is the expected value", () => {
    expect(dateMax).toBeDefined();
  });
});
describe("dateMax() is a function?", () => {
  it("True is the expected value", () => {
    expect(typeof dateMax).toBe("function");
  });
});
